[pytest]
asyncio_mode = auto
env =
    ENVIRONMENT=test
    SECRET_KEY=test-secret-key-do-not-use-in-production
    POSTGRES_DB=onconexus_test_db
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
