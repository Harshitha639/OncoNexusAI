# contexts/

React context providers (e.g. `AuthContext`, `ThemeContext`) will be added
here as their corresponding features are implemented. Kept empty for now
per the project's "foundation only" stage.
